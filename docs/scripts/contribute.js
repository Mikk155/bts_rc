export async function initContributingDoc() {
    const container = document.getElementById("contribute-container");
    if (!container) {
        console.warn("Contribute container not found");
        return;
    }
    //    const res = await fetch( "https://raw.githubusercontent.com/Mikk155/bts_rc/main/CONTRIBUTING.md" );
    //    let html: string = await res.text();
    ley;
    container.innerHTML = html;
}
